[project]
name = "catboost-inference-fixtures"
version = "0.0.0"
description = "Training-side verification harness for catboost-inference (Ruby). Not shipped in the gem."
requires-python = ">=3.11"
dependencies = [
  "catboost==1.2.10",
  "numpy>=1.26",
  "scikit-learn>=1.4",
  "pytest>=8.0",
]
